apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: abdo-user
  namespace: default
spec:
  secretName: abdo-user-tls
  commonName: abdo
  duration: 2160h
  renewBefore: 720h
  issuerRef:
    name: kubernetes-ca-issuer
    kind: Issuer
